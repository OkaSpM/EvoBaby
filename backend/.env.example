LLM_MODE=mock
# API 模式需填写密钥、含版本路径的基础地址及模型名；缺少配置时自动使用离线推理。
OPENAI_API_KEY=
OPENAI_BASE_URL=
MODEL_NAME=
LLM_TIMEOUT_SECONDS=10
LLM_MAX_CONCURRENCY=5
DEFAULT_WORLD_SEED=42

# Optional typed judging gateway; mock remains the offline default.
JUDGE_MODE=mock
JEV_API_KEY=
JEV_API_URL=
